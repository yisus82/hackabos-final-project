'use strict';

const userRouter = require('./user-router');
const mediaInfoRouter = require('./media-info-router');
const reviewRouter = require('./review-router');

module.exports = {
  userRouter,
  mediaInfoRouter,
  reviewRouter,
};
